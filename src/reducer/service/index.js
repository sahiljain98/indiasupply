
import * as type from '../reducerConstants';

const state = {
   
}


const service = (state, action) => {

    switch (action.type) {
        default:
            return state;
    }

}

export default service;